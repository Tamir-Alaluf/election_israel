// Types are generated from prisma/schema.prisma — run `npx prisma generate`; when models are added or removed, update the export list below.

export type {
  ActionGroup,
  BaseTopic,
  BaseTopicOption,
  Candidate,
  CandidateCareerAction,
  CandidateEducation,
  CandidateProfessional,
  CandidateRecentAction,
  FuturePromise,
  GlossaryCategory,
  GlossaryTerm,
  Legislation,
  LegislationGroup,
  LegislationOption,
  Party,
  PartyBaseTopic,
  PartyLegislation,
  PartyMember,
  ProfessionalGroup,
  PublicOpinion,
  RecentAction,
} from "@prisma/client";

export type { Prisma } from "@prisma/client";
